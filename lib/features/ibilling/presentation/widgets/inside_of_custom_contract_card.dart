import 'package:flutter/cupertino.dart';

class InsideOfCustomContractCard extends StatelessWidget {
  const InsideOfCustomContractCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Row(
            children: [],
          )
        ],
      ),
    );
  }
}
